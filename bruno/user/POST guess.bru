meta {
  name: POST guess
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/user/{{userName}}/guess
  body: json
  auth: inherit
}

body:json {
  {
    "guess": "UP"
  }
}

assert {
  res.status: eq 200
  res.body.message: contains success
}

settings {
  encodeUrl: true
  timeout: 0
}
