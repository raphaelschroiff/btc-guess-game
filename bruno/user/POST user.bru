meta {
  name: POST user
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/user
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "userName": "{{$randomUserName}}"
  }
}

assert {
  res.status: eq 201
}
