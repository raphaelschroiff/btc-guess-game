meta {
  name: GET current-price
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/current-price
  body: none
  auth: inherit
}

assert {
  res.body.price: isNumber
}

settings {
  encodeUrl: true
  timeout: 0
}
